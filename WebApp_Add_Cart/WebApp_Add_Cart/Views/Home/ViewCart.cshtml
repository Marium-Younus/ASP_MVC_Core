@* @model Cart

<h2>Shopping Cart</h2>

@if (Model.Products.Any())
{
    <table>
        <thead>
            <tr>
                <th>Product</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in Model.Products)
            {
                <tr>
                    <td>@product.Proname</td>
                    <td>$@product.Proprice</td>
                </tr>
            }
        </tbody>
    </table>

    <p>Total: $@Model.Products.Sum(p => p.Proprice)</p>
}
else
{
    <p>Your cart is empty.</p>
} *@